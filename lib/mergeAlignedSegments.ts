import type { Line } from "./types"

export const mergeAlignedSegments = (segments: Line[]) => {
  // TODO merge lines that are collinear and connected, repeat until no more merges
  return segments
}
